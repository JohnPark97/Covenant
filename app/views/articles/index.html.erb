<h1>Hello, Rails</h1>

<ul>
    <% @articles.each do |article| %>
        <li>
            <a link_to article.title, article %>">
                <%= article.title %>
            </a>
        </li>
    <% end %>
</ul>